version: "1.3.0"
cache: true

interface:
  modelSelect: true
  parameters: true

webSearch:
  serperApiKey: "${SERPER_API_KEY}"
  searchProvider: "serper"
  
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  scraperType: "firecrawl"


  jinaApiKey: "${JINA_API_KEY}"
  rerankerType: "jina"

  scraperTimeout: 7500 # Timeout in milliseconds for scraper requests (default: 7500)
  safeSearch: 1 # Options: 0 (OFF), 1 (MODERATE - default), 2 (STRICT)

  


registration:
  socialLogins: ["openid"]   # Entra only

# Model Specifications for custom names and icons
modelSpecs:
  enforce: false  # Don't force only these models
  prioritize: false  # Allow users to select other endpoints too
  list:
    - name: "grok-4-fast"  # Unique identifier
      label: "Grok 4 Fast"  # Custom display name
      description: "Fast Grok model from X.AI 💰"
      iconURL: "https://upload.wikimedia.org/wikipedia/commons/thumb/5/57/XAI-Logo.svg/250px-XAI-Logo.svg.png"  # Custom icon
      default: true  # Make this the default selection
      preset:
        endpoint: "OpenRouter"  # Must match your custom endpoint name
        model: "x-ai/grok-4-fast"  # Actual model ID for API
        temperature: 0.7
        max_tokens: 4000
        modelLabel: "Grok 4 Fast"
    
    - name: "gpt5-mini"
      label: "GPT-5 Mini"
      description: "Compact version of GPT-5. 💰"
      iconURL: "openAI"  # Use built-in icon
      preset:
        endpoint: "OpenRouter"
        model: "openai/gpt-5-mini"
        modelLabel: "GPT-5 Mini"

    - name: "gpt5"
      label: "GPT-5"
      description: "OpenAI’s most advanced model 💰💰💰"
      iconURL: "openAI"  # Use built-in icon
      preset:
        endpoint: "OpenRouter"
        model: "openai/gpt-5"
        modelLabel: "GPT-5"

    - name: "sonnet4.5"
      label: "Sonnet 4.5"
      description: "Anthropic’s most advanced Sonnet model 💰💰💰💰"
      iconURL: "Anthropic"  # Use built-in icon
      preset:
        endpoint: "OpenRouter"
        model: "anthropic/claude-sonnet-4.5"
        modelLabel: "Sonnet 4.5"

endpoints:
  custom:
    - name: "OpenRouter"
      apiKey: "${OPENROUTER_KEY}"
      baseURL: "https://openrouter.ai/api/v1"
      models:
        default:  # This MUST be an array
          - "x-ai/grok-4-fast"
          - "openai/gpt-4o"
          - "anthropic/claude-sonnet-4.5"
          - "x-ai/grok-code-fast-1"
        fetch: false
      titleConvo: true
      modelDisplayLabel: "OpenRouter"

mcpServers:
  PelsueData:
    name: "Pelsue Data (Readonly)"
    description: "Query Proman data with natural language. Readonly SELECT queries only."
    command: "npx"
    args:
      - "-y"
      - "@supabase/mcp-server-supabase@latest"
      - "--read-only"
      - "--project-ref=${SUPABASE_PROJECT_REF}"
    env:
      SUPABASE_ACCESS_TOKEN: "${SUPABASE_ACCESS_TOKEN}"

