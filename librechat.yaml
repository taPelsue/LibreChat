version: "1.3.0"
cache: true

interface:
  mcpServers:
    placeholder: "MCP Servers"
  privacyPolicy:
    externalUrl: "https://docs.pelsue.com/tools/pelsuechat/"
    openNewTab: true
  termsOfService:
    externalUrl: "https://docs.pelsue.com/tools/pelsuechat/"
    openNewTab: true
    modalAcceptance: true
    modalTitle: "Terms of Service"
    modalContent: |
      # Terms of Service
      ## Introduction
      Welcome to LibreChat!
 

webSearch:
  serperApiKey: "${SERPER_API_KEY}"
  searchProvider: "serper"
  
  firecrawlApiKey: "${FIRECRAWL_API_KEY}"
  scraperType: "firecrawl"


  jinaApiKey: "${JINA_API_KEY}"
  rerankerType: "jina"

  scraperTimeout: 7500 # Timeout in milliseconds for scraper requests (default: 7500)
  safeSearch: 1 # Options: 0 (OFF), 1 (MODERATE - default), 2 (STRICT)

  


registration:
  socialLogins: ["openid"]   # Entra only

endpoints:
  openAI:
    enabled: false
  anthropic:
    enabled: false
  google:
    enabled: false
  custom:
    - name: "OpenRouter"                # appears in the model selector
      baseURL: "https://openrouter.ai/api/v1"
      apiKey: "${OPENROUTER_KEY}"
      models:
        default: [
          "x-ai/grok-4-fast",
          "openai/gpt-4o",
          "openai/gpt-oss-120b",
          "openai/gpt-5-chat",
          "anthropic/claude-sonnet-4.5",
          "google/gemini-2.5-flash"
          ]  # only this model
        fetch: false
      titleConvo: true
      titleModel: "openai/gpt-4o"
      modelDisplayLabel: "Model"   # label on message bubbles


mcpServers:
  PelsueData:
    name: "Pelsue Data (Readonly)"
    description: "Query Proman data with natural language. Readonly SELECT queries only."
    command: "npx"
    args:
      - "-y"
      - "@supabase/mcp-server-supabase@latest"
      - "--read-only"
      - "--project-ref={{SUPABASE_PROJECT_REF}}"
    env:
      SUPABASE_ACCESS_TOKEN: "{{SUPABASE_ACCESS_TOKEN}}"